# ############################ # ############################
# ## Bitnami K8S training
# ## session02/exercise01
# #
# ## Author: Antonio Gamez-Diaz
# ## Contact: antoniogamez@us.es
# #
# # File: wordpress ingres
# # Purpose: redirects "myblog.com" to wordpress
# ############################ # ############################
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: wordpress-ingress
  namespace: s02-exercise-01
  labels:
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/tls-acme: "true"
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true" 
spec:
  tls:
  - hosts:
    - antgamdiapc.lsi.us.es
    secretName: antgamdiapc-lsi-us-es-tls
  rules:
  - host: myblog.com # myblog.com: WordPress
    http:
      paths:
      - path: /
        backend:
          serviceName: wordpress
          servicePort: http
  # - host: drupa